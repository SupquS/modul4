{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_post_edit' %}

{% block main %}
    <h1>{{ 'Edit comment' }}</h1>

    {{ include('admin/category/_form.html.twig', {
        form: form,
        button_label: 'Update',
        include_back_to_home_link: false,
    }, with_context = false) }}
{% endblock %}

{% block sidebar %}
    <div class="section">
        <a href="{{ path('admin_comment_index') }}" class="btn btn-lg btn-block btn-primary">
            <i class="fa fa-list" aria-hidden="true"></i> {{ 'To Comment List' }}
        </a>
    </div>
    {#<div class="section actions">#}
        {#{{ include('admin/category/delete_form.html.twig', { category: category }, with_context = false) }}#}
    {#</div>#}

    {{ parent() }}

{% endblock %}
