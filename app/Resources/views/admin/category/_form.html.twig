{% if show_confirmation|default(false) %}
    {% set attr = {'data-confirmation': 'true'} %}
    {{ include('admin/news/delete_post_confirmation.html.twig') }}
{% endif %}

{{ form_start(form) }}
    {{ form_widget(form) }}

<input type="submit" value="{{ button_label|default('Create category') }}"
       class="{{ button_css|default("btn btn-primary") }}" />

{% if include_back_to_home_link|default(false) %}
    <a href="{{ path('admin_category_index') }}" class="btn btn-link">
        {{ 'Back to List' }}
    </a>
{% endif %}
{{ form_end(form) }}
