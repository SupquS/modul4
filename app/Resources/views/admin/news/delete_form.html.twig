{{ include('admin/news/delete_post_confirmation.html.twig') }}
<form action="{{ url('admin_news_delete', { id: news.id }) }}" method="post" data-confirmation="true">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <input type="submit" value="{{ 'Delete news' }}" class="btn btn-lg btn-block btn-danger" />
</form>
